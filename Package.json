{
  "name": "stable-swap-dex",
  "version": "1.0.0",
  "description": "StableSwap DEX for Arc Testnet",
  "main": "deploy.js",
  "scripts": {
    "deploy": "node deploy.js",
    "setup": "node deploy.js"
  },
  "dependencies": {
    "ethers": "^6.10.0"
  }
}